{
  "name": "page-capture-server",
  "version": "1.0.0",
  "description": "Receives captured pages from the browser extension and saves them as markdown to the vault",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "express": "^4.18.2",
    "jsdom": "^24.0.0",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2"
  }
}
