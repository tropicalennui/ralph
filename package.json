{
  "name": "ralph",
  "version": "1.0.0",
  "description": "Ralph — root test runner",
  "scripts": {
    "test": "node --test mcp-jira/test/**/*.test.js mcp-snow/test/**/*.test.js tools/yoink/server/test/**/*.test.js"
  }
}
