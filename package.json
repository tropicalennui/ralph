{
  "name": "servicenow-claude-integration",
  "version": "1.0.0",
  "description": "ServiceNow–Claude Integration — root test runner",
  "scripts": {
    "test": "node --test mcp-jira/test/**/*.test.js mcp-snow/test/**/*.test.js tools/page-capture/server/test/**/*.test.js"
  }
}
